name: workflow
run-name: ${{ github.actor }} is testing hexlet-ci-app workflow 🚀
on: [push]
jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - name: setup
      run: |
      make setup
      # - run: npm install
      # - run: npm run build
      # - run: npm test
      # - run: npx eslint .
